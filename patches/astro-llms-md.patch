diff --git a/dist/index.js b/dist/index.js
index b9650387de6fabf550045eaad6c4c6943600cbcd..94d8e2089d3503222fc8b79ed8d1acdedec5151c 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -77,7 +77,7 @@ export function normalizePattern(baseDir, pattern) {
     }
     return fullPath;
 }
-export async function discoverHtmlFiles(clientDir, excludePatterns, includePatterns) {
+export function discoverHtmlFiles(clientDir, excludePatterns, includePatterns) {
     const patterns = includePatterns && includePatterns.length > 0
         ? includePatterns.map((p) => normalizePattern(clientDir, p))
         : [path.join(clientDir, "**/*.html")];
@@ -86,7 +86,8 @@ export async function discoverHtmlFiles(clientDir, excludePatterns, includePatte
         ...DEFAULT_EXCLUDES.map((p) => path.join(clientDir, p)),
         ...userExcludes,
     ];
-    let files = await glob(patterns, { ignore, absolute: true });
+    const toGlob = (p) => p.split(path.sep).join("/");
+    let files = glob.sync(patterns.map(toGlob), { ignore: ignore.map(toGlob), absolute: true });
     files = files.filter((f) => fs.statSync(f).isFile() && f.endsWith(".html"));
     return files.sort();
 }
